import path from 'path'

export const GENERAL_CONTEXT_FILE_PATH = path.join(process.cwd(), 'general_context.txt')
export const LOCAL_CONTEXT_FILE_PATH = path.join(process.cwd(), 'local_context.txt')
export const CHAT_HISTORY_FILE = path.join(process.cwd(), 'chat_history.json')
export const AUDIO_HISTORY_FILE = path.join(process.cwd(), 'audio_history.json') 